"""Fixture/placeholder content for pages to render.

Stage 2 (docs/migration.md, sequencing step 2): hard-coded fixture
data standing in for what a real content source (Ghost, or whatever
replaces it) would eventually provide. Nothing here is fetched at
build time -- see `articles.py`.
"""
