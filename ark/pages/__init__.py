"""Page content modules -- one per route.

Each module here exports a plain content-building function (e.g.
`home.home_page()`). The `@site.page(...)` decorators themselves live
in `ark/site.py`, not here -- see `ark/pages/home.py`'s docstring for
why ARKlight requires that split.
"""
